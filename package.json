{
  "name": "crisescalante",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "despegueefiuuuuum": "npm run build && firebase deploy --only hosting",
    "astro": "astro"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "dependencies": {
    "node": "^26.8.2"
  }
}
